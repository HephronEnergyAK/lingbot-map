This directory is the immutable Runtime build input bundled with the Blender
Extension. It contains the exact Worker wheel, frozen uv.lock, schema catalog
and files, Model Catalog, Apache license, and notices. Setup
hashes every declared input before deriving runtime-id and never imports Worker
code into Blender.
