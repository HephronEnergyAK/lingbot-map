# Domain Docs

Before exploring the codebase, read the root `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist. Missing files are not errors; continue silently.

This repository uses a single-context layout:

```
/
├── CONTEXT.md
├── docs/adr/
└── lingbot_map/
```

Use terminology defined in `CONTEXT.md`. Explicitly flag output that conflicts with an existing ADR.
